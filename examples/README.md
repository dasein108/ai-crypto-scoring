# examples/

Two canonical worked dossiers — real `token-research` output, kept as reference specimens (cited across the docs). Not tutorials.

| File | Token case | Commands behind it |
|---|---|---|
| [morpho-deep-analysis.md](morpho-deep-analysis.md) | Mature DeFi lending token | `yields` → `compare` → `risk` → `score` (the run that drove 7 scoring-model fixes) |
| [ondo-analysis.md](ondo-analysis.md) | RWA token (Treasuries + Global Markets) | `risk` / `compare` / `deep-report` |

Reproduce them with the `## Reproducibility` block at the bottom of each file. More case studies (pre-TGE, non-EVM) are under [`../archived/docs/`](../archived/docs/).
